{
  "name": "blogs-frontend",
  "version": "1.0.0",
  "description": "React frontend for blog platform",
  "scripts": {
    "build": "cd web && npm install && npm run build",
    "dev": "cd web && npm start",
    "install-deps": "cd web && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
